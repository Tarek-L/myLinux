# -----------------------------
# Final Setup
# -----------------------------
# Change to home directory when:
# - interactive login shell
# - OR when starting a new tmux session or window
#if [[ (-o login || "$TMUX" == "") && -o interactive ]]; then
#  cd ~
#fi

# Automatically start tmux if not inside tmux, in an interactive shell, and not on a TTY
#if [[ -z "$TMUX" && -o interactive && $(tty) != /dev/tty* ]]; then
#  tmux attach-session -t main 2>/dev/null || tmux new-session -s main
#fi

